export enum InputType {
    EMAIL = "email",
    PASSWORD = "password",
    SUBMIT = "submit",
    TEXT = "text",
    NUMBER = "number",
}

export enum ButtonType {
    PRIMARY = "primary",
    SECONDARY = "secondary",
}

export enum TextTag {
    P = "p",
    LI = "li",
    H1 = "h1",
    H2 = "h2",
    H3 = "h3",
    SPAN = "span",
}
